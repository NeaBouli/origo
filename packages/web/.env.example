# ORIGO Web Client — Feature Flags
# Copy to .env.local for local overrides

VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000

# Lenia Phase 1 — smooth organic rendering (client-side only, zero server cost)
VITE_LENIA_RENDER=true
